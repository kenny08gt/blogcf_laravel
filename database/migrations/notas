para crear migraciones
php artisan <- muestra todos los comandos
php artisan make:migration --help <- muestra como seguir

php artisan make:migration nombre_migracion --create=nombre_tabla_plural

despues para crear las tablas

php artisan migrate


Para crear una muchos a muchos se ccrear una tablas
pivote con el nombre en sigunlar de las dos tablas
tabla1_tabla2


En los modelos fillable indica los campos que pueden ser
manejados para ese objeto, o pueden ser "publicos"